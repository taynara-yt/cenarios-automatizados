
Given(/^que possuo acesso ao sistema$/, () => {
	return true;
});

When(/^clico no botão 'novo cadastro'$/, () => {
	return true;
});

When(/^o sistema apresenta tela para criar conta$/, () => {
	return true;
});

When(/^acesso o formulario de cadastro$/, () => {
	return true;
});

When(/^preencho os campos:$/, () => {
	return true;
});

When(/^clico no botao 'cadastrar'$/, () => {
	return true;
});

Then(/^o sistema retorna a tela de login$/, () => {
	return true;
});


